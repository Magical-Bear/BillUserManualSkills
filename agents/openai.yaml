interface:
  display_name: "Doctor Bill"
  short_description: "Doctor Bill 主入口，负责研发门禁、路由和验收流程"
  default_prompt: "Use $doctor-bill to route my development task through requirements, architecture, implementation, testing, and acceptance gates."
policy:
  allow_implicit_invocation: true
