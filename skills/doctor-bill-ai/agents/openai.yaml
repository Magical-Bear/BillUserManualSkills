interface:
  display_name: "Doctor Bill AI"
  short_description: "RAG、Agent、Dify、推理、微调、评估和 AI 数据治理"
  default_prompt: "Use $doctor-bill first, then route the approved AI portion to $doctor-bill-ai without bypassing requirements or testing gates."
policy:
  allow_implicit_invocation: false
