interface:
  display_name: "Doctor Bill Hardware"
  short_description: "硬件、传感器、串口、MQTT 与设备数据链路设计规范"
  default_prompt: "Use $doctor-bill-hardware to design an ESP32, STM32, sensor, serial, MQTT, and device data pipeline."
policy:
  allow_implicit_invocation: true
