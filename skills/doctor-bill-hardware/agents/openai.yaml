interface:
  display_name: "Doctor Bill Hardware"
  short_description: "硬件、传感器、设备协议、MQTT 和数据链路规范"
  default_prompt: "Use $doctor-bill first, then route the approved hardware portion to $doctor-bill-hardware without bypassing requirements or testing gates."
policy:
  allow_implicit_invocation: false
