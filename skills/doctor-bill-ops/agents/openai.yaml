interface:
  display_name: "Doctor Bill Ops"
  short_description: "systemd、linger、GitHub Actions、部署、健康检查和回滚"
  default_prompt: "Use $doctor-bill first, then route the approved operations portion to $doctor-bill-ops without bypassing approval or safety gates."
policy:
  allow_implicit_invocation: false
