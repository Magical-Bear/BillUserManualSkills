interface:
  display_name: "Doctor Bill Software"
  short_description: "软件、API、SQLAlchemy 异步数据库、报表和 UI 规范"
  default_prompt: "Use $doctor-bill first, then route the approved software portion to $doctor-bill-software without bypassing requirements or testing gates."
policy:
  allow_implicit_invocation: false
